resources:
  jobs:
    one_time_ext_setup:
      name: one-time-ext-setup
      run_as:
        service_principal_name: 35ce9b21-cbf4-4ef4-a672-b3c0c8d14b45
      tasks:
        - task_key: ext_location_setup
          notebook_task:
            notebook_path: /Workspace/Users/dev-user@shantanunarale061gmail.onmicrosoft.com/retail-store-dbricks/retail_store/src/notebooks/setup/00-setup
            source: WORKSPACE
      queue:
        enabled: true
      parameters:
        - name: env
          default: dev
      performance_target: PERFORMANCE_OPTIMIZED        

      # tasks:
      #   - task_key: notebook_task
      #     notebook_task:
      #       notebook_path: ../path/to/notebook.ipynb
      # email_notifications:
      #   on_failure:
      #     - dev-user@shantanunarale061gmail.onmicrosoft.com
      # trigger:
      #   # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
      #   periodic:
      #     interval: 1
      #     unit: DAYS
